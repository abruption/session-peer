# session-peerのリリース

GitHubリリースを公開すると `.github/workflows/publish.yml` がトリガーされ、タグがビルドされ、Trusted Publishingを介してPyPIにアップロードされます。下書きリリースは公開されません。タグとアップロードされた成果物が常にレビュー済みのコードを指すように、リリースの準備、承認、公開、検証を個別のステップとして維持してください。

## 現在の候補: v0.9.0

v0.9.0候補には、承認された #47 アダプター/トランスポートリファクタリング、#69 中継ラボ、#85 Antigravityアダプターに加え、ペアリングされたリクエストをネイティブアダプター経由でルーティングするオプションの製品中継パッケージが統合されています。それ以前の #87/#88 PRは依存関係ブランチにマージされました。それらの承認された変更は、メインターゲットのリリースPRに明示的に引き継がれます。候補ノート: `docs/releases/v0.9.0.md`。公開は #89 で追跡し、成果物の検証が完了するまで開いたままにしてください。
プラグインマニフェストは独立したバージョン（0.1.0）を維持します。

### #90、#91、#92の後の最終統合

リリース確定ブランチは、v0.9機能準備（#90）、サポート/セキュリティパイロット（#91）、および4言語のREADME（#92）を含むmainの `27b28dd` から始まります。後者には、調整されたv0.9の機能説明とsdistの内容が含まれています。パッケージバージョンはすでに `0.9.0` です。このドキュメント確定のためにバージョンを再度インクリメントしないでください。このベースは最終的なリリースコミットではありません。確定PRがマージされた後の正確なmainコミットを記録してください。

- #89 を開いたままにしておくこと: 準備PRのマージは公開や成果物の検証ではありません。
- 翻訳された3つのREADMEすべて、`docs/cli-reference.md`、セキュリティポリシー、およびv0.9リリースノートを、オプションの中継ファイルとともにsdistに含めること。
- 独立したプラグインバージョン、凍結されたレガシーアップデータ、オプションのエクストラ、および実験コードの除外を維持すること。CIでwheelとsdist両方のインストールを検証すること。
- この確定作業の準備時点での最新の公開リリースはv0.8.0でした。候補の文言をPyPIへのアップロード成功と解釈してはなりません。
- マージ後にのみ下書きを作成すること。リリースの公開には、後述する個別の最終承認が依然として必要です。このPRから #89 を自動的にクローズしないでください。

relayエクストラにはUnixおよびPython 3.11以上が必要です。依存関係のないコアに加えて、`docs/relay-public-pilot-2026-09-17.md` にある公開パイロットの証拠を含め、`.[relay,mcp]` を検証してください。限定的なライブテストはソークテストではありません。実際の運用検証は引き続きv1.0.0-rcに向けた関門です。

リポジトリには、従来の自己更新URL用に凍結されたルートの `cc_peer.py` を引き続き含める必要があります。これはsession-peerのwheelおよびsdistの外部に維持しなければなりません。

## Trusted Publisherの設定

GitHub環境は `pypi` であり、アクティブなワークフローは `publish.yml` です。PyPIプロジェクトオーナーは、このTrusted Publisherマッピングを維持する必要があります:

| フィールド | 値 |
| --- | --- |
| PyPI project | `session-peer` |
| GitHub owner | `abruption` |
| GitHub repository | `session-peer` |
| Workflow filename | `publish.yml` |
| GitHub environment | `pypi` |

v0.6.1リリースではこのパスが正常に使用されました。GitHubはPyPIオーナー側のマッピングを検査できないため、前回の成功はそれが変更されていないことの保証ではなく証拠となります。

## 準備と検証

1. 現在の `origin/main` からリリースissueとブランチを作成します。
2. 1つのリリース準備PRで、`session_peer.__version__`、永続的なREADMEの文言、このランブック、および `docs/releases/<version>.md` を更新します。
3. CIで使用されるチェックを実行します:

   ```bash
   python3 -m compileall -q cc_peer.py session_peer.py session_peer_mcp.py tests
   python3 -m unittest discover -s tests -v
   python3 -m unittest discover -v
   python3 -m build
   ```

   `.[mcp]` エクストラがインストールされた別のPython 3.10以上の環境でテストスイートを再実行します。スタンドアロン環境では、オプションのSDKテストのみがスキップされるはずです。リリースチェックのためにライブのwake/モデルテストを有効にしないでください。

4. 両方のアーカイブを検査します。`session_peer.py`、ライセンス、メタデータ、およびREADMEはsdistに含まれます。wheelには `session_peer.py`、`session_peer_mcp.py`、オプションの `session_peer_relay/` パッケージ、およびメタデータが含まれます。sdistにはMCP/wake/マルチホームのドキュメントおよびプラグインファイルも含まれます。どちらのアーカイブにも `cc_peer.py`、認証情報、セッションデータベース、またはローカルノートを含めてはなりません。
5. クリーンな環境にwheelとsdistを個別にインストールします。`session-peer --version`、`session-peer list --output-format json`、およびインポートメタデータを確認します。セッションなしのスモークチェックには、分離された一時HOMEを使用してください。エクストラをインストールした状態で `session-peer-mcp --help` を確認します。デフォルトポリシーはローカル読み取り専用のままでなければなりません。
6. 必要なすべてのチェックに合格した後にのみ、リリース準備PRをマージします。`main` をフェッチし、その正確なコミットを記録し、意図した変更とバージョンが引き続き含まれていることを確認します。

## 下書きの準備

スカッシュまたはマージコミットによってリリースコミットが変更されるため、下書きはリリース準備PRがマージされた後にのみ作成してください:

```bash
git fetch origin main --tags
release_commit=$(git rev-parse origin/main)
gh release create v0.9.0 \
  --repo abruption/session-peer \
  --target "$release_commit" \
  --title "session-peer v0.9.0" \
  --notes-file docs/releases/v0.9.0.md \
  --draft --latest
```

下書きのタグ、ターゲットコミット、タイトル、ノート、下書きステータス、およびプレリリースステータスを確認します。下書きの保存時にGitHubがタグを作成した場合は、記録されたリリースコミットに解決されることを確認してください。既存の公開済みタグを移動しないでください。

## 公開

公開の直前に最終承認を取得します。公開はGitHubリリースを一般公開し、PyPIアップロードを開始するアクションです:

```bash
gh release edit v0.9.0 --repo abruption/session-peer --draft=false --latest
```

ワークフローが失敗した場合、2つ目のリリースを作成したり、変更された成果物で再試行したりしないでください。失敗した実行を保持し、失敗したステージを診断してください。PyPIによってすでに受け入れられたバージョンを置き換えることはできません。

## 公開の検証

1. リリースによってトリガーされた `publish.yml` の実行が正常に完了し、期待されるタグとコミットが使用されたことを確認します。
2. PyPIが正確に `session-peer==0.9.0` を公開していることを確認します。wheelとsdistをダウンロードし、そのファイル名とSHA-256ハッシュをワークフローの成果物と比較して、内容を再度検査します。
3. クリーンな環境にPyPIから0.9.0をインストールします。バージョンとローカルの読み取り専用一覧を確認します。送信を行わないドライランでは、明示的な一時Codexホームと実行可能ファイルを使用できます。キューコマンドが実行されなかったことが証明されるのであれば、予期された対象の失敗は許容されます。
4. GitHubがv0.9.0をlatestとしてマークし、古いスタンドアロンインストールに対して `session-peer update --check` がそれを報告することを確認します。
5. GitHub、PyPI、新規インストール、およびアップデータの検証が記録された後にのみ、リリースissueをクローズします。

パッケージマネージャーで管理されたインストール環境は、各マネージャーを使用してアップグレードします。スタンドアロンインストールでは `session-peer update` を使用します。同梱のClaudeスキルを更新するには `install.sh` が必要です。送信は消費や確認応答の証明には決してなりません。
