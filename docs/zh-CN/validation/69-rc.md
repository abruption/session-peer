# #69 RC 验证记录（进行中）

这是一份开发记录，而非 RC 发布批准。实际的 GitHub/Google 登录、原生注册/准入、公共收据恢复、配对的公共 E2EE 中继以及隔离的 Codex/Antigravity 副本 ACK 已在下文记录的范围内通过。观察于 9 月 18 日 18:18 KST 开始，并在大约 5 小时 21 分钟后根据用户要求停止；用户免除了 24 小时要求。它未通过 24 小时运行测试。9 月 19 日的最终 RC 重新运行涵盖了实际的 Claude、Codex 和 Antigravity 确认，包括看门狗触发后的公共中继确认。前述章节描述了在指定时间的观察结果，在后续结果明确说明之处被取代。

## 候选版本与范围

随后的控制看门狗更改取代了 `9c62f9e` 作为生产代码候选版本；其先前的结果仅保留为该特定修订版本的证据。KR 发现 `Upholds` 恢复了退出的进程，但留下了被 SIGSTOP 冻结的控制和服务中继。该应用程序现在根据监听器和持久公共状态的健康状况来门控 systemd 通知；参见[看门狗操作](../relay-watchdog.md)。本地类型检查/构建以及 63 项控制测试均已通过。后续的实际 systemd 恢复和部署报告记录在下方，与本地模拟分开。在该候选验证阶段，尚未开始 24 小时浸泡测试。

KR 验证者随后报告了对精确看门狗候选版本 `428575a11799e4eb15b43e86aa6ceebe648dd7d4` 的独立检查：在 Linux ARM64 Node 22.23.2 和 24.16.0 上干净的 npm 安装、类型检查/构建以及各 63 项测试均获通过；Python 3.12.3 安装/pip 检查以及所有四项 Node/Python 集成测试均获通过（10.351 秒）。归档字节、符号链接和锁定文件保持不变。这些集成测试包括轮换提交后的托管 WebSocket 断开连接、同操作恢复/重复数据删除以及打开连接的撤销。这些测试使用合成凭据和原生 fixture，而非实时 OAuth 或模型。早期的 402 项测试套件、审计和发布冒烟测试在此次有限的后续测试中未重复进行，绝不能归因于此确切修订版本。

报告的 KR 证据：`/home/ubuntu/session-peer-69-verify-428575a-ja2rrmku/verification-result.json`，SHA256 `a263539246e473c9251e9d0d7ac5a1afafd44b5010031e0df391533fd7a3b7a3`。此独立报告未确立 systemd 通知归属、SIGSTOP 恢复、生产应用或公共入口网关。

## 实际 KR 看门狗与初始公共入口（操作员报告）

Mac 本体基础设施操作员随后在独立的 KR DynamicUser/state 沙箱中测试了确切的 `428575a`，并报告所有运行时门控均已通过：Node MAINPID/WATCHDOG_PID 归属、通知确认、35.02 秒健康运行、控制/中继崩溃恢复以及中继重启前的新就绪状态。整个 cgroup 的 SIGSTOP 在约 29.95 秒内达到看门狗故障；持久公共状态不匹配在约 32.43 秒内产生健康状态 503 和看门狗故障。两者均自动恢复了控制和中继。显式堆栈停止保持停止状态。配置的 300 秒/5 次启动限制阻止了单独的重复故障测试。密钥、重放证据和修订历史均得以保留；迁移/重放初始化并非自动执行。

证据：`/opt/session-peer-watchdog-test.yqQsro/watchdog-result.json`，SHA256 `e616e0afb74d70401f6a45e6659d03ee2d67c37223d5b9794a4f534da71ab746`。这些是操作员实际的 systemd 观察结果，而非声明 root 会话在生产环境中独立重复了故障注入。

同一操作员安装了不可变的 `428575a` 版本以及经审查的看门狗/堆栈单元，保留了现有状态并创建了受保护的一致身份验证数据库快照。未执行任何迁移、重放重新初始化或开机自启启用。生产就绪状态和 35 秒健康运行均已通过。托管的 Caddy 块在实际 Caddy 用户验证和实时配置哈希检查后应用。现有站点基线保持不变。Caddy 哈希：`78596de9a349a7df94226591830f40de4344a2b3b04d59a7b020d8425b25bf95`。操作员观察到实际的 GitHub 同意/回调到达已通过身份验证的空设备页面。在完成经过验证的 sub 发现之前，Google 仍保持禁用状态。

当前阻碍项：原生 `POST /api/auth/device/code` 被 Cloudflare 拒绝。root 会话于 2026-09-18 08:08:02Z 独立复现了一次请求：HTTP 403，Cloudflare 错误 1010（`browser_signature_banned`），Ray `a3cedc4558521c66-HKG`。响应声明其不可重试。未使用浏览器 Cookie 传输、伪装的用户代理或重复尝试。CLI 登录令牌创建、代码批准、设备注册和已认证的公共 WSS 仍未验证。成功的浏览器登录或 `/healthz` GET 不能作为其证据。该确切 Ray 随后匹配到一条 Cloudflare 安全事件，属性为 `source=bic`、`ruleId=bic`、`action=block`。在获得用户明确批准后，操作员针对确切的 `/api/auth/device/code` 和 `/api/auth/device/token` 路径（位于 `relay.abruption.dev`），为不带查询参数的 HTTPS POST 添加了一条仅针对 BIC 的配置规则。参见[边缘策略](../relay-edge-policy.md)。规则 `7c74849bd67d4efaa459864ee7eaf8dc` 属于规则集 `d63e46bcdd88453bae8f67c2ecbba8a7`，创建时两者均为版本 1。root 会话读取了操作员保存的结果和 Trace 记录：两个预期路径均匹配，而六个排除的情况均未匹配。报告指出其他保护措施保持不变。实时原生授权和下游路由结果仍与此配置检查分开；不包含其他路由例外。

随后，阶段 A 通过了实际的原生接收方登录：代码发放、现有浏览器中的代码/应用程序比对、显式批准、正常间隔轮询、exit 0，以及在 0700 设备目录下由模式 0600 保护的 `login.json`。基础设施操作员在未使用伪装的用户代理或浏览器 Cookie 传输的情况下执行并报告了此实时测试。此处未记录代码/令牌值。源工件保持为 `428575a`。

随后单次的登记尝试在 2026-09-18T08:18:30Z 于 POST `/api/relay/challenge` 处被阻止，HTTP 403，Ray `a3ceeb9d38d49840-HKG`。随后的只读查询找到了属性为 `source=bic`、`ruleId=bic`、`action=block` 的确切安全事件；首次的空查询结果未被视为不存在的证据。登记操作 ID `cddec6ac-ab54-4fef-872d-b58c8ae70359` 保留用于恢复，未被替换。注册/准入/Cookie/WSS 尚未运行。成功的两条路径例外仍然有效；未向被阻止的路径自动扩展。

root 读取的证据：`/private/tmp/session-peer-replay-infra-20260918.07q6VB/BIC-STAGE-A-RESULT.md` 和 `bic-stage-a/challenge-ray-followup.json`。报告指出现有站点基线和其他 Cloudflare 设置保持不变。任何额外例外均需要所有者的单独授权。

### 获批的注册/准入例外与登记 CLI 修正

所有者批准了在原有两条路径之外增加 POST `/api/relay/challenge`、`/api/relay/devices` 和 `/api/relay/admission`。在 2026-09-18T08:34:25Z，相同的规则和规则集升级为版本 2，包含五个精确的 HTTPS POST/无查询路径。仅包含表达式的 API PATCH 最初返回 400 且未更改规则；随后操作员提供了未更改的必填字段，仅在语义上更改了表达式。GET 比对报告其他策略保持不变。root 读取了存储的 v2 结果以及所有 15 个成功的 Trace 案例（5 个匹配，10 个排除）。

接收方复用了其初始登记操作 ID。挑战返回 200，注册返回 201，带 `committed=true` 且世代为 0；浏览器设备页面也显示了该设备。原生准入返回 200，GET `/v1/session` 返回 200 并附带 Cookie。未添加任何 GET/WebSocket 例外。

这暴露了一个 CLI 缺陷：`enroll()` 返回了已提交的控制收据但未包含 `ok`，而 CLI 退出状态仅检查 `ok`。因此成功的注册以状态码 1 退出。修正方案根据原始操作/主体/证书/世代验证全新和对账后的收据，丢弃不相关的响应字段，并在本地登记结果中添加 `ok=true`。一项真实的 Node HTTP CLI 测试验证了初始成功和同操作对账均以 exit 0 退出，且收据完全相同，没有额外的公共状态发布。格式错误的收据在保存登记元数据之前会被拒绝。未通过使用新操作 ID 重复进行生产注册来规避此报告缺陷。修正后的本地验证：11 项针对性的控制/Node 集成测试以及包含 404 项测试的完整 Python 套件均获通过（后者耗时 18.047 秒）。控制服务的 Node 源代码与 `428575a` 相比未作更改；未对此仅针对 CLI 的修正声明任何新的 Node 套件或 systemd 故障测试结果。

首次仅接收方的 WSS 尝试超时。其插桩仅记录了异常类型，因此 HTTP 升级和超时阶段均未得到证实。中继在发送 `relayAttached` 之前会等待双方角色；缺少客户端是一种可能的解释，而非经证实的诊断。配对的 fixture 测试必须区分升级、挂载、内部 E2EE 和原生 fixture 交付。这些观察结果并未确立真实的模型调用或 24 小时运行。

操作员证据：`/private/tmp/session-peer-replay-infra-20260918.07q6VB/BIC-STAGE-B-RESULT.md`、`bic-stage-b/{result,trace-result}.json` 以及 `/private/tmp/session-peer-public-wss.1Vmb9F/` 下经净化的接收方登记/准入观察记录。

操作员随后使用独立的 Python 3.14 运行时将 CLI 候选版本 `102be87` 针对未更改的 `428575a` 后端进行了验证。归档和捆绑包哈希匹配，安装和 pip 检查通过，已通过身份验证的 fixture 客户端以其固定的操作 ID 进行了注册：挑战 200、devices 201、`ok=true`、`committed=true`、世代 0 且 exit 0。这证实了公共注册路径上的退出状态修正。

接收方原始操作的对账达到了预期的挑战 409，随后 GET `/api/relay/operations/{original-id}` 返回 Cloudflare 403（Ray `a3cf10b85fa82da1-NRT`）。未重复进行任何注册变更。该 GET 在批准的五个 POST 例外之外。首次只读事件查询达到了单独的 GraphQL 配额限制，因此导致此新增 403 的规则尚未确立。两台设备存储的收据仍保持在世代 0 的已提交状态。公共对账成功和配对 WSS 仍未验证；CLI fixture 对账结果不能替代其中任何一项。

证据：`/private/tmp/session-peer-public-client-102be878.vTklvu/` `PUBLIC-ENROLLMENT-FIX-RESULT.md` 及其净化的登记/Ray 观察记录。独立的配对测试可以复用这两个已完成的注册，而无需重复被阻止的收据查询；该测试指令未授权额外的边缘例外。

### 配对的公共 WSS fixture 证据

在 2026-09-18 08:49:34–08:49:42 UTC，Mac 操作员使用本地客户端/接收方 `102be87` 和后端 `428575a` 完成了一次有限的配对公共测试。root 读取了报告和经净化的观察 JSON，并验证了其报告的 SHA256 哈希；它并未独立重新运行网络实验。配对首先使用 localhost 直接 E2EE。随后，两个已注册的角色通过正常的 TLS/主机名验证连接到公共中继。这是公共中继传输证据，而非公共配对或脱离 VPN 认证。

第一次交换以及显式的相同消息 ID 重复均各自观察到了两次 HTTP 101 升级（共四次）、中继挂载以及内部 TLS 握手，客户端检查了其预期的对端固定证书。产品原生工作器/套接字适配器将预期正文传递给确定性的 Unix 套接字 fixture。第二个请求返回了 `duplicate=true`：产生了一次原生效应和一次完成的持久接收方收据。`consumptionConfirmed=false` 保持不变。未调用任何真实代理或模型（包括 Claude）。

一项单独的有限公共准入检查对无效凭据返回 401，对同一凭证的首次有效凭据返回 200，对已消耗凭证的重放返回 401。未发生额外的 CF403、重新连接或后端/边缘策略更改。这并未确立所有的安全、轮换、重启或长时间运行门控。Fixture 工作器/监听器已停止，Store 已关闭，Unix 套接字已被移除；端点身份、配对和收据均予以保留。

现有的 operations GET403 仍未解决。在超过 5 分钟后进行的第二次只读事件查询再次返回了 GraphQL 配额错误，因此其来源/规则/动作仍属未知，未应用进一步的例外。Google OAuth、公共收据对账、实际代理 ACK 和持续 24 小时运行测试仍处于待处理状态。

`/private/tmp/session-peer-public-client-102be878.vTklvu/` 下的证据：
`PAIRED-PUBLIC-WSS-RESULT.md` SHA256
`d831da4258dcffe53fc212da8c32e31748c814f5dbf406948a735b54f6e37060`；
`paired-public-observation.json` SHA256
`56e08f49f755070b64e3fc63dc62c9b068f6dc7518ca22b965b30cac80d117fd`。
### 实际原生 ACK 与 Google OAuth

9 月 18 日，为 Codex 0.155.0 和 Antigravity CLI 1.2.4 创建了两个新的隔离 macOS TUI 对话。接收端的 Antigravity TUI 通过其自身经过身份验证的命令工具启动了有界桥接。未提取或复制任何凭据。在桥接启动前，操作员最初的路径拼写错误导致失败，并在交付前得到纠正。

使用端点 `102be87` 和后端 `428575a`，每个目标接收了一条直接 localhost 消息和一条公共中继消息。在目标的助手/模型脚本记录中独立观察到了四个不同的精确 ACK 标记。Codex 返回了队列接受，Antigravity 返回了原生提交；产品仍报告 `consumptionConfirmed=false`。脚本 ACK 是独立的证据，并非对该 API 契约的更改。未调用任何 Claude 进程/模型。随后退出了这两个测试 TUI；Antigravity 所有者退出时移除了其桥接注册和套接字。这涵盖了同一 Mac 原生端点，不包括 Linux 或 MacBook 目标、禁用 VPN 或故障发生后的原生 ACK。

证据：`/private/tmp/session-peer-69-live-ack-anddz4_x/result.json`，SHA256
`03099f2ae89cf89e0d2084db6118ddce4927e7b137757a500abbc6ad46e011ea`，
以及那里的四个传输结果和过滤后的精确 ACK 脚本记录。
最初的直接 CLI 桥接停止报告了 `not_registered`；但在正常所有者 TUI 退出后确认了清理。导入的适配器/原生工作线程发现和发送均获成功；不同的独立发现观察结果不会被默许视为已通过的独立列表/停止检查。

基础设施操作员分别完成了实际 Google 回调发现、服务身份下验证主体持久化确认、精确主体白名单添加、发现设置移除，以及随后的正常 Google 登录和显式原生设备代码批准。Google 成为了独立的所有者；账户关联保持禁用状态，GitHub 设备/收据保持完好。原始 GitHub 凭据收到 200，而 Google 凭据针对 GitHub 所有者的收据收到 404。未声称任何 Google 设备注册。在持续观察活动之前进行了配置重启。

报告：`/private/tmp/session-peer-public-client-102be878.vTklvu/A-B-AUTHORIZED-RESULT.md`，
SHA256 `3fb0e38caf77220c46726f81bd7705def53ad128310651a0363e5de99335d917`。
root 读取了脱敏报告；提供商/浏览器执行由基础设施操作员执行。这些结果并未确立 Claude 验证或 24 小时活动的完成。

### 公共收据恢复已完成

随后的原生收据查找返回了 Cloudflare 错误 1010。由于诊断 GraphQL 预算仍处于耗尽状态，未能直接获取原始 Ray 的安全事件源。仅限 BIC 的前/后实验恢复了原始接收端 UUID 路由，随后批准的动态路由限制于 09:12:40.517 UTC 作为相同规则的版本 4 应用。它保留了五个 POST 路径，并且仅添加了在 `/api/relay/operations/` 下包含规范小写 UUIDv4 的中继主机 HTTPS GET/无查询路径。显式检查了长度、十六进制、连字符、版本和变体；未使用无限制的 API 前缀或付费正则表达式升级。不受支持的替代方案未经更改即被拒绝。

Trace 通过了 21 个预期/排除的用例。另一现有客户端的原始操作随后通过 challenge 409 → 认证收据 GET 200（CLI 退出码 0）进行了对账。未使用任何注册突变或新操作 ID。查询 GitHub 收据的 Google 所有者以及查询不存在规范 ID 的授权所有者均收到 404。这取代了早先未解决的公共收据观察结果，同时保留了其证据和诊断限制。

证据：同一 public-client 目录下的 `DYNAMIC-RECEIPT-BIC-RESULT.md`，
SHA256 `b7a0bfd03277fa0eeb109223e5275c12a1ca3fedeaeb46c80a5ef1af26d6e25f`；
`dynamic-receipt-native-result.json` SHA256
`2b677132cdfe1f4fdeb47731642ad235b3c8ff161b992229f44bf170842839a8`。
基础设施操作员运行了这些检查；root 读取了脱敏报告。

### 持续观察 — 因用户请求而停止

端点活动实际上于 **2026-09-18 09:18:00.1365 UTC**（18:18 KST）开始，其 24 小时端点截止时间为 9 月 19 日同一时间。KR 只读观察者的 systemd 启动时间戳独立显示为 9 月 18 日 18:18:00 KST，处于 active/running 状态。这记录的是开始而非完成。端点 `102be87`、后端 `428575a`、BIC 规则 v4 以及最终 Google 配置为基线。上述实际模型 ACK 发生在活动之前。

固定测试套件每分钟对经过身份验证的 localhost 直接/公共 WSS 探测进行采样，并每五分钟发送一次确定性原生夹具消息。另一路由上的相同 ID 必须抑制重复效应。它不发起模型调用；全天预算约为 288 个新收据。未知/失败的原生发送将停止观察且不重试，连续三个失败的探测周期亦然。样本间隔予以保留，不通过追赶请求进行掩盖。首个样本：两个探测均通过，四次 HTTP101 升级，一个新原生效应和一个成功的重复响应，零失败/间隔。

这两个登录凭据在活动结束前到期。正常的浏览器批准设备代码重新授权定于 T+20h（9 月 19 日 14:18 KST），若到期时间要求则更早。测试套件排队了一个协调请求；排队并不证明已续期。两个新的有效期都必须覆盖活动结束时间。不允许手动延长令牌有效期、状态重置或凭据复制。如果续期失败，由此产生的中断/失败将保留在证据中。

运维文件：`/private/tmp/session-peer-69-soak-20260918/`，包含用于进度/证据的
`campaign-20260918T091800Z/status.json` 和 `samples.jsonl`。
测试套件 SHA256 `0752f8b280aa57e828cafae8c5034c3fb934f5bda935d6e443a84a7ffea15330`。
最终测试套件首先完成了一个 70 秒的冒烟测试，包含两个成功周期、六次 HTTP101 升级、一个效应和一个重复项；它正确标记了 `passed24Hours=false`。独立的分离有界启动器可在运行时防止空闲休眠，并将完成/中断摘要排入队列供审查。它不是安装的引导服务或 cron 作业。进程持久性并不是重启保证。

服务器观察者为 `session-peer-soak-observer-20260918.service`；其基线和只读资源采样器位于
`/var/lib/session-peer-deployments/soak-preparation-20260918/` 目录下。实际经过时间后必须对端点和服务器证据同时进行审查。此观察本身并不涵盖 Linux/MacBook 接收端、禁用 VPN、所有计划的故障或 Claude ACK。更广泛的发布门禁仍然是分开的；未发布任何版本。

`/var/lib/session-peer-deployments/428575a11799e4eb15b43e86aa6ceebe648dd7d4/` 下的生产结果文件：
`watchdog-loopback-result.json` SHA256
`e1863c396888721ee25c7e357f83f531df2983705c5ee00793e3a7770f98578d`，以及
`caddy-public-result.json` SHA256
`535617e7a8d8593806327a37fb5eea151208fd471c8375602b9cf71508ea2f33`。
这些生产部署结果文件不涵盖实际代理调用或随后的 24 小时活动。此记录未确立任何 RC 发布、防火墙/DNS 变更、全区域保护放宽或备份作业变更。

基线：`0dd9592d8dfddc56ecbca54072a8733afd998d37`（v0.9.0 加文档）。
开发分支：`feat/69-relay-rc-hardening`。
保留原始工作检出。控制服务是单独构建的 Node 应用程序；安装 Python CLI 并不会安装或启动它。

批准的 RC 范围包括 React、Better Auth、GitHub 和 Google 登录、设备所有权与设备绑定准入、密钥生命周期，以及现有 Caddy 入口之后的专用 KR 部署。初始账户已加入白名单。公共主机名并不意味着开放注册或提供托管服务。

## 验收映射

| Issue #69 criterion | Implementation / test evidence | Remaining RC evidence |
| --- | --- | --- |
| 默认仅本地操作及本地/SSH 兼容性 | 现有 CLI 默认路由、405 测试套件、全新 wheel 冒烟测试及 KR 源流 SSH 列表 | 更广泛的支持主机包冒烟测试 |
| 通过直接路由允许的原生目标 | 原生夹具以及实际同一 Mac Claude、Codex 和 Antigravity 直接 ACK | 更广泛的接收端平台 |
| 通过不透明中继的相同身份 | TLS 1.3 端点流、配对的公共夹具以及实际同一 Mac Claude、Codex 和 Antigravity 中继 ACK | 更广泛的实际设备/关闭 VPN 覆盖 |
| 结构化提交阶段 | 提交结果与独立脚本 ACK 分开捕获；`consumptionConfirmed` 保持为 false；故障后 Claude ACK 通过 | 更广泛的平台覆盖 |
| 并发路由选择 | `exchange()` 探测在一次提交前验证候选对象 | 故障和关闭 VPN 路由观察 |
| 共享版本化契约与衔接就绪状态 | Node/Python 集成以及实际配对的公共连接/内部 TLS | 超出此 macOS 试点的支持平台覆盖 |
| 配对/重放/撤销/轮换/限制 | 原生/中继、生命周期和准入测试，以及实际重放/撤销和看门狗恢复 | 免除 24 小时要求 |
| 丢失响应对账 | Node/Python 轮换丢失响应测试及实际同操作公共注册 GET 恢复 | 更广泛的故障/重连及恢复演练 |
| 中继中断隔离 | 直接回退和 CLI 回归测试 | 包含显式 SSH 冒烟测试的公共服务停止/重启 |
| 平台生命周期 | Relay 额外组件支持 Unix/Python 3.11+、显式管理命令 | macOS 和 Linux 候选生命周期；Windows 中继仍不受支持 |
| 威胁模型/设置/移除/恢复 | `paired-devices.md`、`relay-lifecycle.md`、`relay-auth.md`、部署的单元哈希以及实际看门狗恢复 | 更广泛的主机丢失演练 |

Windows 保留核心本地/SSH 支持；此变更不声称支持 Windows 接收端或中继。已记录的仅限 Unix 边界必须在 RC 描述中保持明确，而不是将 Windows 中继集成标记为已通过。

## 本地发现与修复

独立的本体论审查复现了三个故障：恢复可能会在隔离前暴露状态、恢复的发送端仍可发送/轮换，以及缺失的中继重放文件可被自动重新创建。Python 修复 `52d32c6` 在发布前暂存恢复、检查发送端和接收端的恢复边界，并要求显式的持久重放初始化。

回归覆盖范围还检查了中断的恢复、保留的状态查找、缺失/损坏的重放状态、引导票据隔离、无法持久化已使用票据，以及跨重启的公共状态回滚。同一台机器上的高水位线无法检测到所有权威机构的完整机器回滚；主机丢失恢复仍需要外部对账/隔离。

此修复后本地 macOS/Python 3.13 运行情况：发现 399 个测试，396 个通过，3 个 Node 集成测试因等待通用 API 契约而明确跳过。Wheel 和 sdist 构建通过。初始 Node 候选在 macOS 上独立通过了类型检查/构建以及 31 个测试；KR 报告在 Linux ARM64 Node 22/24 上进行了相同的 31 个测试。这些计数并非实时 OAuth 或模型验证。

精确的 Python 提交 `52d32c6` 也被归档并安装到包含中继和 MCP 额外组件的隔离 KR Ubuntu ARM64/Python 3.12 虚拟环境中。所有 396 个测试均在 36.611 秒内通过。在保留测试日志后移除了临时源/环境。此次运行不涉及任何模型调用、生产接收端、中继服务或 Caddy 变更。

`tests/control_integration/test_control_integration.py` 下的 Node/Python 测试使用真实的控制应用程序、Better Auth、SQLite 和密码学以及一次性种子会话。它们的环回 HTTP 桥接仅替换外部 TLS 传输。生产应用程序中未添加任何测试身份验证端点。集成候选最初通过了三项测试：零代注册/准入/撤销、包含丢失提交响应和原生效应重复数据删除的托管密钥轮换，以及跨所有者接收端拒绝。共享契约在首次注册时省略 expectedGeneration，并在首次轮换时使用 expectedGeneration=0/新 generation=1。历史上已完成的操作通过仅限所有者的收据进行对账。

随后的 macOS 候选运行通过了启用集成的全部 401 个 Python 测试、52 个 Node 测试、类型检查/构建以及生产 npm audit（零报告漏洞）。生产者回归测试包括限制性 umask、独立数据库重启、可见发布后的目录 fsync 失败、修订耗尽以及恢复/缺失修订拒绝。Google 引导测试包括部分写入/文件 fsync/链接失败、模糊目录持久化和显式操作员确认。这些是夹具结果，并非实时提供商登录。

第四个跨语言测试随后通过了完整的托管准入 → cookie → WebSocket → 固定端点 TLS → 原生夹具路径。捕获的中继帧不包含该标记，控制撤销关闭了已打开的发送端连接并释放了其中继插槽。仅外部网络 TLS 被重定向到环回；这并不能证明公共 WSS。全新的 wheel 和 sdist 核心安装也通过了版本/帮助冒烟测试，无额外组件套件通过了 338 个测试（跳过 8 个可选模块）。

KR 生产者工作 df66f9a/1336ee0 提供了公共目录权限和修订故障测试，并与 Python 世代契约和现有公共文件回滚保护相集成。最终的集成候选仍需要独立的 Linux 验证和实际的沙箱部署。

进一步的回调回归重现了未加入白名单的身份遗留孤立用户行的问题，即使账户创建和登录均被拒绝。现在，GitHub 和经过验证的 Google 用户信息处理程序都会在 Better Auth 创建用户之前检查不可变提供商白名单；数据库钩子仍作为第二道边界。回归断言涵盖了两个提供商均未更改的用户/账户/会话计数。KR 随后的 3de5604 还贡献了证书派生的初始主体验证以及拒绝已填充的预契约候选状态的模式标记。最终的 Node 套件通过了 55 个测试，并且全部四个 Python 集成用例再次通过。这些发现取代了用于实际 OAuth 部署的早期候选。
## 独立的 Linux 与服务证据

KR 在 Linux ARM64 上独立验证了确切候选版本 `54f09f6`：
Node 22.23.2 和 24.16.0 分别通过了 npm ci、typecheck、build、52 项测试以及
生产依赖审计（零报告漏洞）。Python 3.12.3 在启用了当时有效的 3 项 Node 集成测试的
情况下通过了全部 401 项测试。原始 KR 源码分支保持未修改。这是该早期候选版本的
证据，而非后续 OAuth 和初始主体（initial-principal）修正的证据。

最新的生产代码候选版本 `9c62f9e` 在 macOS 上通过了 402 项 Python 测试、
55 项 Node 测试、typecheck/build 以及 wheel/sdist 构建。其 4 项集成测试包含
托管 WebSocket 路径。最新的 KR 服务沙箱分别通过了编译迁移、
环回 health/UI/Host 及未认证检查、跨 UID 就绪状态（cross-UID readiness）、
显式重放初始化、重放保留、版本号（revision）递增以及依赖启动/停止行为。
其提供方传输（provider-transport）测试夹具通过了 18 项 OAuth/发现测试；
实际提供方登录仍未测试。

沙箱发现了两项部署要求：Node 可执行文件必须是受保护主目录路径之外、
由 root 所有的常规运行时，且中继（relay）重放状态必须使用专用子目录，
而非 DynamicUser 顶级符号链接。暂存的单元已作相应调整。当前 control 崩溃会
导致 relay 停止；仅恢复 control 并不能使其重启。自动恢复仍然是一个
关卡（gate），而非将手动启动视为自动恢复。沙箱单元和监听器已被停止/移除，
共享的 Caddy 配置保持不变。这些检查使用的是合成凭据和隔离状态，
而非生产 OAuth 凭据或真实 agent 会话。

随后，最新的确切候选版本 `9c62f9e` 通过了独立的 KR Linux ARM64 验证：
Node 22.23.2 和 24.16.0 分别通过了全新安装、typecheck、build、55 项测试以及
零报告漏洞的生产审计。Python 3.12.3 在 35.992 秒内通过了包括当时 4 项集成用例在内的
全部 402 项测试。源码字节、lockfile 以及原始 KR 分支均得到保留。
这些结果来自独立的 KR 会话，而非第二次 macOS 运行。

后续的一项仅限测试的扩展在托管 WebSocket 路由本身上演练了轮换。
在端点提交密钥变更后，测试夹具在返回响应之前关闭其实际的 WebSocket。
复用操作 ID（operation ID）可对齐 control 回执与端点状态，激活新密钥，
并保留原生消息回执：重新发送相同的消息 ID 总共只产生一次效果。
4 项重点集成用例在 macOS 上用时 5.267 秒通过。刻意设置的恢复阶段间隔
超出了中继每秒 20 次握手的预算；生产速率限制既未被提高也未被绕过。
该测试夹具仍以环回替代外部 TLS，并且不建立公共 WSS 或 OAuth。

## 最终活动决策与运维交接

用户放弃了 24 小时测试，并要求终止环回和网络测试进程。端点测试活动在
19,253.8356 秒后于 **2026-09-18 14:38:54.010369 UTC**（23:38:54 KST）结束。
其最终状态为 `interrupted`：321 个周期，321 次直接探测和 321 次中继探测成功，
65 次新消息和 65 次重复消息成功，以及 772 次公共 WebSocket 升级。
记录的故障、安全故障和样本缺漏均为零。`passed24Hours` 和 `releaseGatePassed`
仍为 false。这是部分观察证据，而非完整的 24 小时通过。

本地启动器/端点测试框架以及 KR 观察者服务/定时器已停止。先前的重新授权计划
已随本次测试活动一同取消；此项交接未安排进一步的测试或凭据更新。
KR control/relay 服务与这些已终止的测试进程保持分离。CF 基础设施采纳计划
已被废弃，并从本 KR 实现分支和推送中排除。

已审查的 KR watchdog/readiness/relay 单元及辅助脚本的无密钥副本保存在
[deploy/ops/abruption-kr](../../../deploy/ops/abruption-kr/README.md) 中。它们不包含凭据、环境文件、
提供方主体 ID、数据库或重放状态。记录的实际 OAuth、transcript ACK 和
服务故障证据仍受限于上述范围。停止耐受（soak）测试并不会扩大这些范围，
且本次源码推送并非 RC 发布批准。

## 最终 RC 重新运行 — 9 月 19 日（不含 24 小时测试）

用户要求进行仅排除连续 24 小时测试的完整 RC 重新运行。最终候选版本在
macOS Python 3.14.7 环境下以及针对已部署的 KR 服务进行了测试。
control 项目通过了 `npm ci`、typecheck、build、8 个文件中的 63 项测试以及
零报告漏洞的 `npm audit --omit=dev`。Python 套件在启用实际编译的 Node control
集成的情况下通过了全部 405 项测试。构建了全新的 sdist 和 wheel；wheel 连同
relay 和 MCP 额外组件（extras）安装到了新的虚拟环境中，`pip check` 通过，
安装的 CLI 报告为 0.9.0，且干净主目录（clean-home）列表冒烟测试通过。
通过 SSH 到 KR 的源码流式执行返回了结构化的成功列表。

重新运行暴露并修复了两项 Antigravity 1.2.7 兼容性故障。原生 `agentapi send-message`
命令在其接收者和消息位置参数之前不再接受多余的 `--`。桥接器之前还比对了
按语言环境格式化的 `ps lstart` 字符串，因此在韩国语 TUI 中启动的桥接器会在
中继工作进程使用 C 语言环境时消失。该命令现在使用文档中说明的位置参数，
并且进程启动标识始终使用 `LC_ALL=C` 读取。添加了精确的 argv 和稳定语言环境
回归测试。统一列表测试现在显式隔离可选的活跃 Antigravity 注册，且先前的
Claude 权限错误测试已调整为与当前的故障闭合（fail-closed）发现契约保持一致。

每次原生尝试均使用了全新的唯一请求 ID。在同一 Mac 上的直接传递和
公共中继传递均顺利通过，涵盖 Claude 2.1.277、使用 `gpt-5.6-sol high` 的
隔离 Codex 0.155.0 会话，以及使用 Gemini 3.8 Flash High 的 Antigravity CLI 1.2.7。
每次公共中继交换均观察到了接收方附加以及原生提交或队列接收。每个确切标记在
相应的助手或模型 transcript 中各出现了一次。这些 transcript 观察结果仍属于独立证据；
产品 API 继续报告 `consumptionConfirmed=false`。

在注入故障之前，control、readiness、relay 和 stack 单元均处于活跃状态，
health 返回 200，公共状态/重放高水位版本号均为 1183。全进程 `SIGSTOP`
触发了所配置的 30 秒看门狗（watchdog）。在 35.67 秒内，control 在新 PID 下重启，
readiness 再次运行，relay 在新 PID 下重启且无需手动恢复。配置哈希保持不变；
状态和重放版本号推进至 1185 且未发生回退，两个环回监听器均已恢复，
health 返回
200. 恢复后新的 Claude 公共中继请求收到了一个确切的 transcript ACK。
在最终空闲采样中，control 使用了约 39.8 MiB 及 11 个任务，relay 使用了
约 19.8 MiB 及 1 个任务；不存在已建立的 3769/3770 连接。最近的 3 条 control
警告均为预期的注入看门狗超时、SIGKILL 以及结果失败记录；relay 没有警告级别的日志行。

前面各节中记录的实际 GitHub 和 Google 回调、提供方所有者隔离、原生设备授权、
注册/对齐（enrollment/reconciliation）、配对的公共 WSS/E2EE、无效证明与重放拒绝，
以及活动连接撤销证据，仍然适用于未变更的已部署后端。最终的重新运行未重复
提供方授权同意或更改提供方身份。未执行任何合并、发布标签创建、软件包发布或 24 小时测试活动。

## KR 开机启动激活与基础设施重启 — 9 月 19 日

`session-peer-stack.target` 在开机启动策略中仅由 `multi-user.target` 启用。
control 和 relay 服务作为直接开机入口点保持禁用状态；target 启动 control，
并通过现有的 Upholds、readiness、Requires 和 BindsTo 关系确立
control → readiness → relay 的顺序。迁移和重放初始化仍为手动的、
非活跃的一次性操作，不是开机启动依赖项。

一次实际的 KR 主机重启将 boot ID 从
`2537e227-6dc3-4137-85c6-bd253e4f8e5f` 变为
`5523b4cb-68d6-40a0-8987-b1cf084857f6`。SSH 在约 28 秒内恢复。
control 于 12:37:38 KST 启动，于 12:37:49 通过其 health 和状态关卡，
readiness 完成，relay 于 12:37:49 无需人工干预启动。全部 4 个 stack 单元均处于
活跃状态，零重启且无失败的系统单元。公共 health 和登录返回 200；
未认证的会话和挑战请求分别继续被拒绝为 401 和 403。

认证状态版本号从 1205 推进至 1207。重放文件保留了其 2 条已使用记录和
原始 `notBefore`，然后在 relay 启动时将其高水位版本号从 1185 推进至 1207。
重启期间未运行任何迁移、重放初始化、凭据替换或数据库重置。

开机启动后检查了现有基础设施：Caddy、Authelia、Authadmin、Ontology、
作品集仪表盘、服务器监控、Orca、Redis、两个 rclone 挂载、Tailscale、
SSH、OCI 监控以及中央 Vault 运行时均处于活跃状态。两个 rclone 挂载已
成功挂载且可读。公共 ontology、chat、login 和 status 路由均返回了预期的
302/200/200/302 状态，且 `vault
status` reported `integrity=ok`。没有预期或正在运行的容器。唯一的开机警告是
先前已存在的内核/平台和 OCI 单元模式警告；开机过程中未产生失败的单元。

现有的 KR Restic 任务最初没有 session-peer 路径。首次冷快照尝试因故障闭合（failed closed）
而失败，因为停止 target 并未在立即检查前使 control 变为非活跃状态。
该尝试未产生快照或远程备份，且辅助脚本的清理恢复了全部 4 个单元。修正后的
辅助脚本显式地按倒序停止 relay、readiness、control 和 target，并等待它们
全部变为非活跃状态后才读取持久化状态。

修正后的运行创建了冷快照 `20260919T034516Z`：SQLite 完整性为 `ok`；
存在 2 台设备、2 张操作回执、2 位用户和 4 个会话；公共状态版本号为 1214；
重放高水位为 1213 且保留了全部两张已使用票据；准入签名密钥与发布的 JWKS
相匹配。快照完成后 stack 恢复健康状态，无需执行迁移或重放初始化。

随后常规加密 R2 任务以退出码 0 完成，并存储了 Restic 快照
`d286abf2841aa5ef6bc92b00d0b6cadc34b441ef51fdaa12c043011a3482eb42`。
其范围包含冷快照、受保护的 control 配置、全部 4 个单元、辅助脚本、
readiness 代码以及不可变的 control/relay 运行时。在保留和修剪（prune）之后，
仓库在共享的 10 GiB 配额中占用了 0.9407 GiB。

该确切的 R2 快照被恢复到了一个仅限 root 的隔离目录中。Restic 下载了
总计 180.638 MiB 的 3,742 个条目。对清单中全部 10 个持久化文件及模式、
SQLite 完整性及表计数、签名密钥/JWKS 一致性、2 台设备、2 张回执、
公共版本号 1214、包含 2 条已使用记录的重放版本号 1213、所需单元/辅助脚本
以及两个发布符号链接的验证均告通过。测试未写入生产路径。恢复后的副本
随后被移除；线上 DB 完整性仍为 `ok`，stack 保持健康，未出现失败单元。
每日 05:00 任务现会调用冷快照辅助脚本，并包含经过验证的恢复范围。

## 发布关卡

1. 共享注册（enrollment）、证明（proof）、轮换、生成（generation）、房间（room）、
   证书指纹以及公共状态版本号契约通过了最终集成测试套件。
2. 实际的 GitHub/Google 回调、显式设备批准、所有者隔离以及
   通过 React 界面进行的撤销在记录的生产试运行中通过。
3. 修复的工件、KR 隔离/就绪状态（readiness）/状态持久化、Caddy 入口（ingress）
   以及看门狗恢复在配置和服务证据得到保留的情况下通过。
4. 用户放弃了连续 24 小时的要求。保留已中断的测试活动证据；
   在不将已中断的测试活动视为通过的前提下，单独完成约定的其余故障/恢复和平台检查。
5. 针对隔离的 Claude、Codex 和 Antigravity 的实际目标 transcript/ACK 证据
   在最终重新运行中通过。保留唯一请求记录，切勿重新发送任何未知的历史尝试。
6. 仅在约定的关卡全部通过时方可提交 RC 发布 PR。此项授权不包括
   合并发布 PR、创建发布标签或发布到 PyPI。

无论是测试夹具套接字写入还是 Codex 队列接收，都不能证明已完成消费（consumption）。
保留未知结果和原始操作 ID；绝不能仅为了将未知结果变为成功报告而重新提交。
