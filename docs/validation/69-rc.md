# #69 RC validation record (in progress)

This is a development record, not an RC release approval. Actual GitHub/Google
login, native registration/admission, public receipt recovery, paired public
E2EE relay and isolated Codex/Antigravity transcript ACKs have passed within the
scopes recorded below. The observation started on September 18 at 18:18 KST and was stopped
at the user's request after approximately 5 hours 21 minutes; the user waived
the 24-hour requirement. It did not pass a 24-hour campaign. A final RC rerun
on September 19 covered actual Claude, Codex and Antigravity acknowledgements,
including a post-watchdog public-relay acknowledgement. Earlier sections describe
observations at their stated times, superseded where later results explicitly say so.

## Candidate and scope

The subsequent control watchdog change supersedes `9c62f9e` as a production-code
candidate; its previous results remain evidence for that exact revision only.
KR found that `Upholds` recovered exited processes but left a SIGSTOP-frozen
control and relay active. The application now gates systemd notifications on
listener and durable public-state health; see [watchdog operation](../relay-watchdog.md).
Local typecheck/build and 63 control tests pass. The later actual systemd recovery
and deployment report is recorded below, separately from the local mocks.
At that candidate-validation stage, no 24-hour soak had started.

The KR validator subsequently reported independent checks of exact watchdog
candidate `428575a11799e4eb15b43e86aa6ceebe648dd7d4`: clean npm installations,
typecheck/build and 63 tests each on Linux ARM64 Node 22.23.2 and 24.16.0;
Python 3.12.3 installation/pip check and all four Node/Python integration tests
passed (10.351 seconds). Archive bytes, symlinks and lockfile stayed unchanged.
Those integration tests include the managed WebSocket disconnect after rotation
commit, same-operation recovery/deduplication and open-connection revocation.
These use synthetic credentials and native fixtures, not live OAuth or models.
The earlier 402-test suite, audit and release smoke were not repeated in this
limited follow-up and must not be attributed to this exact revision.

Reported KR evidence: `/home/ubuntu/session-peer-69-verify-428575a-ja2rrmku/verification-result.json`,
SHA256 `a263539246e473c9251e9d0d7ac5a1afafd44b5010031e0df391533fd7a3b7a3`.
This independent report does not establish systemd notification attribution,
SIGSTOP recovery, production application or the public ingress gate.

## Actual KR watchdog and initial public ingress (operator report)

The Mac ontology infrastructure operator subsequently tested exact `428575a` in
an independent KR DynamicUser/state sandbox and reported all runtime gates passed:
Node MAINPID/WATCHDOG_PID attribution, notification acknowledgement, 35.02 seconds
healthy operation, control/relay crash recovery, and fresh readiness before relay
restart. Whole-cgroup SIGSTOP reached watchdog failure in approximately 29.95 seconds;
persistent public-state mismatch produced health 503 and watchdog failure in
approximately 32.43 seconds. Both automatically recovered control and relay.
Explicit stack stop remained stopped. The configured 300-second/five-start limit
blocked the separate repeated-failure test. Keys, replay evidence and revision
history were preserved; migration/replay initialization were not automatic.

Evidence: `/opt/session-peer-watchdog-test.yqQsro/watchdog-result.json`, SHA256
`e616e0afb74d70401f6a45e6659d03ee2d67c37223d5b9794a4f534da71ab746`.
These are the operator's actual systemd observations, not a claim that the root
session independently repeated fault injection on production.

The same operator installed immutable `428575a` releases and the reviewed
watchdog/stack units, preserving existing state and taking a protected consistent
auth database snapshot. No migration, replay reinitialization or boot enablement
was performed. Production readiness and 35-second healthy operation passed.
The managed Caddy block was applied after actual-caddy-user validation and a live
configuration hash check. Existing site baselines remained unchanged. Caddy hash:
`78596de9a349a7df94226591830f40de4344a2b3b04d59a7b020d8425b25bf95`.
The operator observed an actual GitHub consent/callback reaching the authenticated
empty device page. Google remains disabled pending verified-sub discovery.

Current blocker: native `POST /api/auth/device/code` is rejected by Cloudflare.
The root session independently reproduced one request on 2026-09-18 at 08:08:02Z:
HTTP 403, Cloudflare error 1010 (`browser_signature_banned`), Ray
`a3cedc4558521c66-HKG`. The response declares it non-retryable. No browser-cookie
transfer, disguised user-agent or repeated attempts were used. CLI login token
creation, code approval, device enrollment and authenticated public WSS remain
unverified. A successful browser login or `/healthz` GET is not evidence for them.
The exact Ray was subsequently matched to a Cloudflare security event with
`source=bic`, `ruleId=bic`, `action=block`. After explicit user approval, the
operator added one BIC-only Configuration Rule for HTTPS POSTs without a query to
the exact `/api/auth/device/code` and `/api/auth/device/token` paths on
`relay.abruption.dev`. See [the edge policy](../relay-edge-policy.md).
Rule `7c74849bd67d4efaa459864ee7eaf8dc` belongs to ruleset
`d63e46bcdd88453bae8f67c2ecbba8a7`, both version 1 at creation.
The root session read the operator's saved result and Trace records: both intended
paths matched, while six excluded cases did not. Other protection was reported
unchanged. Live native authorization and downstream route results remain separate
from this configuration check; no other route exception is included.

Stage A then passed actual native receiver login: code issuance, code/application
comparison in the existing browser, explicit approval, normal-interval polling,
exit 0 and a `login.json` protected by mode 0600 under a 0700 device directory.
The infrastructure operator performed and reported this live test without a
disguised user-agent or browser-cookie transfer. Code/token values are not recorded
here. The source artifact stayed `428575a`.

The subsequent single enrollment attempt was blocked at POST
`/api/relay/challenge`, HTTP 403, Ray `a3ceeb9d38d49840-HKG`, at
2026-09-18T08:18:30Z. A later read-only lookup found the exact security event with
`source=bic`, `ruleId=bic`, `action=block`; the first empty lookup was not treated
as proof of absence. Enrollment operation ID
`cddec6ac-ab54-4fef-872d-b58c8ae70359` is retained for resumption, not replaced.
Registration/admission/cookie/WSS have not run. The successful two-path exception
remains in place; there was no automatic extension to the blocked path.

Evidence read by root:
`/private/tmp/session-peer-replay-infra-20260918.07q6VB/BIC-STAGE-A-RESULT.md`
and `bic-stage-a/challenge-ray-followup.json`. Existing site baselines and other
Cloudflare settings were reported unchanged. Any additional exception requires
separate owner authorization.

### Approved registration/admission exception and enrollment CLI correction

The owner approved POST `/api/relay/challenge`, `/api/relay/devices` and
`/api/relay/admission` in addition to the original two paths. On
2026-09-18T08:34:25Z the same rule and ruleset became version 2, with five exact
HTTPS POST/no-query paths. An expression-only API PATCH first returned 400 without
changing the rule; the operator then supplied the unchanged required fields and
changed only the expression semantically. GET comparisons reported other policy
unchanged. The root read the stored v2 result and all 15 successful Trace cases
(five matches, ten exclusions).

The receiver reused its original enrollment operation ID. Challenge returned 200,
registration 201 with `committed=true` and generation 0; the browser device page
also showed the device. Native admission returned 200 and GET `/v1/session`
returned 200 with a cookie. No GET/WebSocket exception was added.

This exposed a CLI defect: `enroll()` returned the committed control receipt
without `ok`, while CLI exit status checked only `ok`. Thus successful registration
exited 1. The correction validates fresh and reconciled receipts against the
original operation/principal/certificate/generation, drops unrelated response
fields, and adds `ok=true` to the local enrollment result. A real Node HTTP CLI
test checks initial success and same-operation reconciliation both exit 0, with
identical receipts and no additional public-state publication. Malformed receipts
are rejected before saving enrollment metadata. No production registration is
repeated with a new operation ID to work around this reporting defect.
Local verification after the correction: 11 focused control/Node integration
tests and the full 404-test Python suite passed (18.047 seconds for the latter).
The control service's Node source is unchanged from `428575a`; no new Node suite
or systemd fault-test result is claimed for this CLI-only correction.

The first receiver-only WSS attempt timed out. Its instrumentation recorded only
the exception type, so neither HTTP upgrade nor the timeout stage was proven.
The relay waits for both roles before sending `relayAttached`; absence of a client
is a possible explanation, not a verified diagnosis. A paired fixture test must
distinguish upgrade, attachment, inner E2EE and native fixture delivery. No real
model call or 24-hour campaign is established by these observations.

Operator evidence:
`/private/tmp/session-peer-replay-infra-20260918.07q6VB/BIC-STAGE-B-RESULT.md`,
`bic-stage-b/{result,trace-result}.json`, and sanitized receiver enrollment/admission
observations under `/private/tmp/session-peer-public-wss.1Vmb9F/`.

The operator subsequently verified CLI candidate `102be87` against the unchanged
`428575a` backend using a separate Python 3.14 runtime. The archive and bundle
hashes matched, installation and pip check passed, and the already-authenticated
fixture client registered with its fixed operation ID: challenge 200, devices
201, `ok=true`, `committed=true`, generation 0 and exit 0. This confirms the exit
correction on the public registration path.

Reconciliation of the receiver's original operation reached the expected
challenge 409, then GET `/api/relay/operations/{original-id}` returned Cloudflare
403 (Ray `a3cf10b85fa82da1-NRT`). No registration mutation was repeated. That GET
is outside the five approved POST exceptions. The first read-only event lookup
hit a separate GraphQL budget limit, so the rule responsible for this new 403
was not established. Stored receipts for both devices remained committed at
generation 0. Public reconciliation success and paired WSS remain unverified;
the CLI fixture reconciliation result does not substitute for either.

Evidence: `/private/tmp/session-peer-public-client-102be878.vTklvu/`
`PUBLIC-ENROLLMENT-FIX-RESULT.md` and its sanitized enrollment/Ray observations.
The independent paired test can reuse the two completed registrations without
repeating the blocked receipt lookup; no additional edge exception is authorized
by that test instruction.

### Paired public WSS fixture evidence

On 2026-09-18 at 08:49:34–08:49:42 UTC, the Mac operator completed a limited
paired public test using local client/receiver `102be87` and backend `428575a`.
The root read the report and sanitized observation JSON and verified their
reported SHA256 hashes; it did not independently rerun the network experiment.
Pairing first used localhost direct E2EE. Both already-registered roles then
connected to the public relay with normal TLS/hostname verification. This is
public relay transport evidence, not public pairing or VPN-off certification.

The first exchange and an explicit same-message-ID duplicate each observed both
HTTP 101 upgrades (four total), relay attachment and inner TLS handshakes, with
the client checking its expected peer pin. The product native worker/socket
adapter delivered the expected body to a deterministic Unix-socket fixture.
The second request returned `duplicate=true`: there was one native effect and
one completed durable receiver receipt. `consumptionConfirmed=false` remained
unchanged. No real agent or model was invoked, including Claude.

A separate limited public admission check returned 401 for an invalid proof,
200 for the same ticket's first valid proof, and 401 for replay of the consumed
ticket. No additional CF403, reconnect or backend/edge-policy change occurred.
This does not establish all security, rotation, restart or long-duration gates.
Fixture workers/listeners were stopped, Stores closed and the Unix socket
removed; endpoint identities, pairing and receipts were preserved.

The existing operations GET403 remains unresolved. A second read-only event
lookup after more than five minutes again returned a GraphQL budget error, so
its source/rule/action is still unknown and no further exception was applied.
Google OAuth, public receipt reconciliation, actual agent ACKs and the continuous
24-hour campaign remain pending.

Evidence under `/private/tmp/session-peer-public-client-102be878.vTklvu/`:
`PAIRED-PUBLIC-WSS-RESULT.md` SHA256
`d831da4258dcffe53fc212da8c32e31748c814f5dbf406948a735b54f6e37060`;
`paired-public-observation.json` SHA256
`56e08f49f755070b64e3fc63dc62c9b068f6dc7518ca22b965b30cac80d117fd`.

### Actual native ACKs and Google OAuth

On September 18, two new isolated macOS TUI conversations were created for
Codex 0.155.0 and Antigravity CLI 1.2.4. The receiving Antigravity TUI started
the bounded bridge through its own authenticated command tool. No credentials
were extracted or copied. An initial operator path typo failed before bridge
startup and was corrected before delivery.

Using endpoint `102be87` and backend `428575a`, each target received one direct
localhost and one public-relay message. Four distinct exact ACK markers were
independently observed in the targets' assistant/model transcript records.
Codex returned queue acceptance and Antigravity native submission; the product
still reported `consumptionConfirmed=false`. The transcript ACKs are independent
evidence, not a change to that API contract. No Claude process/model was invoked.
The two test TUIs were then exited; Antigravity owner exit removed its bridge
registration and socket. This covers same-Mac native endpoints, not Linux or
MacBook targets, VPN disabled, or post-fault native ACKs.

Evidence: `/private/tmp/session-peer-69-live-ack-anddz4_x/result.json`, SHA256
`03099f2ae89cf89e0d2084db6118ddce4927e7b137757a500abbc6ad46e011ea`,
plus the four transport results and filtered exact-ACK transcript records there.
The initial direct CLI bridge stop reported `not_registered`; cleanup was instead
confirmed after normal owner TUI exit. Imported adapter/native worker discovery
and sends succeeded; the differing standalone discovery observation is not
silently treated as a passed standalone list/stop check.

The infrastructure operator separately completed real Google callback discovery,
verified-subject persistence confirmation under the service identity, exact
subject allowlisting, removal of discovery settings, and a subsequent normal
Google login and explicit native device-code approval. Google became a separate
owner; account linking stayed disabled, and the GitHub devices/receipts remained
intact. Google credentials received 404 for the GitHub owner's receipt while
the original GitHub credentials received 200. No Google device enrollment was
claimed. Configuration restarts preceded the continuous observation campaign.

Report: `/private/tmp/session-peer-public-client-102be878.vTklvu/A-B-AUTHORIZED-RESULT.md`,
SHA256 `3fb0e38caf77220c46726f81bd7705def53ad128310651a0363e5de99335d917`.
The root read the sanitized report; provider/browser execution was performed by
the infrastructure operator. These results do not establish Claude validation
or completion of the 24-hour campaign.

### Public receipt recovery completed

The later native receipt lookup returned Cloudflare error 1010. Because the
diagnostic GraphQL budget remained exhausted, the original Ray's security-event
source was not directly obtained. A BIC-only before/after experiment restored
the original receiver UUID route, then the approved dynamic route restriction
was applied as version 4 of the same rule at 09:12:40.517 UTC. It preserves the
five POST paths and adds only relay-host HTTPS GET/no-query paths containing a
canonical lowercase UUIDv4 under `/api/relay/operations/`. Length, hex, hyphens,
version and variant are explicitly checked; no unrestricted API prefix or paid
regex upgrade was used. An unsupported alternative was rejected without change.

Trace passed 21 intended/excluded cases. The other existing client's original
operation then reconciled through challenge 409 → authenticated receipt GET 200
with CLI exit 0. No registration mutation or new operation ID was used. A Google
owner querying the GitHub receipt and an authorized owner querying a nonexistent
canonical ID each received 404. This supersedes the earlier unresolved public
receipt observations, while preserving their evidence and diagnostic limits.

Evidence: `DYNAMIC-RECEIPT-BIC-RESULT.md` under the same public-client directory,
SHA256 `b7a0bfd03277fa0eeb109223e5275c12a1ca3fedeaeb46c80a5ef1af26d6e25f`;
`dynamic-receipt-native-result.json` SHA256
`2b677132cdfe1f4fdeb47731642ad235b3c8ff161b992229f44bf170842839a8`.
The infrastructure operator ran these checks; root read the sanitized report.

### Continuous observation — stopped by user request

The endpoint campaign actually started at **2026-09-18 09:18:00.1365 UTC**
(18:18 KST), with its 24-hour endpoint deadline on September 19 at the same time.
The KR read-only observer's systemd start timestamp independently showed
September 18 18:18:00 KST, active/running. This records a start, not completion.
Endpoint `102be87`, backend `428575a`, BIC rule v4 and the final Google configuration
are the baseline. The actual model ACKs above precede the campaign.

The fixed harness samples authenticated localhost direct/public WSS probes every
minute and sends a deterministic native fixture message every five minutes.
The same ID on the other route must suppress a duplicate effect. It makes no
model calls; roughly 288 new receipts are budgeted over the day. Unknown/failed
native sends stop observation without retry, as do three consecutive failed
probe cycles. Sample gaps are retained, not hidden by catch-up requests. First
sample: both probes passed, four HTTP101 upgrades, one new native effect and
one successful duplicate response, with zero failures/gaps.

The two login credentials expire before the campaign ends. Normal browser-approved
device-code reauthorization is due at T+20h (September 19 14:18 KST), or earlier
if expiry requires it. The harness queues one coordination request; queueing does
not prove renewal. Both new expiries must cover the campaign end. No manual token
expiry extension, state reset or credential copy is permitted. If renewal fails,
the resulting interruption/failure remains in the evidence.

Operational files: `/private/tmp/session-peer-69-soak-20260918/`, with
`campaign-20260918T091800Z/status.json` and `samples.jsonl` for progress/evidence.
Harness SHA256 `0752f8b280aa57e828cafae8c5034c3fb934f5bda935d6e443a84a7ffea15330`.
The final harness first completed a 70-second smoke with two successful cycles,
six HTTP101 upgrades, one effect and one duplicate; it correctly marked
`passed24Hours=false`. A detached bounded launcher prevents idle sleep while
running and queues a completion/interruption summary for review. It is not an
installed boot service or cron job. Process persistence is not a reboot guarantee.

The server observer is `session-peer-soak-observer-20260918.service`; its baseline
and read-only resource sampler are under
`/var/lib/session-peer-deployments/soak-preparation-20260918/`. Endpoint and server
evidence must both be reviewed after actual elapsed time. This observation does
not by itself cover Linux/MacBook receivers, VPN disabled, every planned fault,
or Claude ACKs. Broader release gates remain separate; no release was published.

Production result files under
`/var/lib/session-peer-deployments/428575a11799e4eb15b43e86aa6ceebe648dd7d4/`:
`watchdog-loopback-result.json` SHA256
`e1863c396888721ee25c7e357f83f531df2983705c5ee00793e3a7770f98578d`, and
`caddy-public-result.json` SHA256
`535617e7a8d8593806327a37fb5eea151208fd471c8375602b9cf71508ea2f33`.
Those production deployment result files do not cover actual agent calls or the
later 24-hour campaign. No RC publication, firewall/DNS change, zone-wide
protection relaxation or backup-job change is established by this record.

Base: `0dd9592d8dfddc56ecbca54072a8733afd998d37` (v0.9.0 plus documentation).
Development branch: `feat/69-relay-rc-hardening`.
The original working checkout is preserved. The control service is a separately
built Node application; installing the Python CLI does not install or start it.

The approved RC scope includes React, Better Auth, GitHub and Google login,
device ownership and device-bound admission, key lifecycle, and a private KR
deployment behind the existing Caddy ingress. Initial accounts are allowlisted.
The public hostname does not imply open registration or a hosted service offer.

## Acceptance mapping

| Issue #69 criterion | Implementation / test evidence | Remaining RC evidence |
| --- | --- | --- |
| Default local-only operation and local/SSH compatibility | Existing CLI default routing, 405-test suite, fresh wheel smoke and KR source-stream SSH listing | Wider supported-host package smoke |
| Permitted native targets through direct route | Native fixtures plus actual same-Mac Claude, Codex and Antigravity direct ACKs | Wider receiver platforms |
| Same identity via opaque relay | TLS 1.3 endpoint stream, paired public fixture and actual same-Mac Claude, Codex and Antigravity relay ACKs | Wider actual-device/VPN-off coverage |
| Structured submission stages | Submission results and independent transcript ACKs captured separately; `consumptionConfirmed` stays false; post-fault Claude ACK passed | Wider platform coverage |
| Concurrent route selection | `exchange()` probes verified candidates before one submission | Fault and VPN-off route observations |
| Shared versioned contract and splice readiness | Node/Python integration plus actual paired public attachment/inner TLS | Supported-platform coverage beyond this macOS pilot |
| Pairing/replay/revocation/rotation/limits | Native/relay, lifecycle and admission tests plus actual replay/revoke and watchdog recovery | 24-hour requirement waived |
| Lost response reconciliation | Node/Python rotation lost-response tests and actual same-operation public enrollment GET recovery | Wider fault/reconnect and recovery rehearsal |
| Relay outage isolation | Direct fallback and CLI regression tests | Public service stop/restart with explicit SSH smoke |
| Platform lifecycle | Relay extra supports Unix/Python 3.11+, explicit management commands | macOS and Linux candidate lifecycle; Windows relay remains unsupported |
| Threat model/setup/removal/recovery | `paired-devices.md`, `relay-lifecycle.md`, `relay-auth.md`, deployed unit hashes and actual watchdog recovery | Wider host-loss rehearsal |

Windows keeps core local/SSH support; this change does not claim Windows receiver
or relay support. The documented Unix-only boundary must remain explicit in the
RC description, rather than marking Windows relay integration as passed.

## Local findings and corrections

The independent ontology review reproduced three failures: restore could expose
state before quarantine, restored senders could still send/rotate, and a missing
relay replay file could be recreated automatically. The Python correction
`52d32c6` stages restoration before publication, checks both sender and receiver
recovery boundaries, and requires explicit durable replay initialization.

Regression coverage also checks an interrupted restore, retained status lookup,
missing/corrupt replay state, bootstrap ticket fencing, failure to persist a spent
ticket, and rollback of public state across restart. A complete machine rollback
of all authorities cannot be detected by a high-water mark on that same machine;
host-loss recovery still requires external reconciliation/fencing.

Local macOS/Python 3.13 run after this correction: 399 discovered tests, 396
passed and 3 Node integration tests explicitly skipped pending the common API
contract. Wheel and sdist builds passed. The initial Node candidate independently
passed typecheck/build and 31 tests on macOS; KR reported the same 31 tests on
Linux ARM64 Node 22/24. These counts are not live OAuth or model validation.

The exact Python commit `52d32c6` was also archived and installed into an isolated
KR Ubuntu ARM64/Python 3.12 virtual environment with relay and MCP extras. All
396 tests passed there in 36.611 seconds. The temporary source/environment was
removed after preserving the test log. No model call, production receiver,
relay service or Caddy change was part of this run.

Node/Python tests under `tests/test_control_integration.py` use the real control
application, Better Auth, SQLite and cryptography with disposable seeded sessions.
Their loopback HTTP bridge replaces only the external TLS transport. No test
authentication endpoint is added to the production application. The integrated
candidate initially passed three tests: generation-zero enrollment/admission/revocation,
managed key rotation with lost commit response and native-effect deduplication,
and cross-owner receiver rejection. The shared contract omits expectedGeneration
on first registration and uses expectedGeneration=0/new generation=1 on first
rotation. Historical completed operations reconcile via the owner-only receipt.

The subsequent macOS candidate run passed all 401 Python tests with integration
enabled, 52 Node tests, typecheck/build and a production npm audit (zero reported
vulnerabilities). Producer regressions include restrictive umask, separate-DB
restart, a directory-fsync failure after visible publication, revision exhaustion
and restored/missing revision refusal. Google bootstrap tests include partial
write/file-fsync/link failures, ambiguous directory persistence and explicit
operator confirmation. These are fixture results, not live provider login.

A fourth cross-language test then passed the complete managed admission → cookie
→ WebSocket → pinned endpoint TLS → native fixture path. The captured relay frames
did not contain the marker, and control revocation closed an already-open sender
connection and released its relay slot. Only outer network TLS was redirected to
loopback; this does not prove public WSS. Fresh wheel and sdist core installs also
passed version/help smoke, and the no-extras suite passed 338 tests (8 optional
module skips).

KR producer work df66f9a/1336ee0 supplied the public-directory permissions and
revision fault tests, integrated with the Python generation contract and existing
public-file rollback guard. The final integrated candidate still needs independent
Linux verification and actual sandbox deployment.

A further callback regression reproduced an unallowlisted identity leaving an
orphan user row even though account creation and login were refused. Both GitHub
and verified Google user-info handlers now check the immutable provider allowlist
before Better Auth creates a user; database hooks remain a second boundary.
Regression assertions cover unchanged user/account/session counts for both
providers. KR's subsequent 3de5604 also contributed certificate-derived initial
principal validation and a schema marker that refuses populated pre-contract
candidate state. The resulting Node suite passed 55 tests and all four Python
integration cases passed again. These findings supersede the earlier candidate
for actual OAuth deployment.

## Independent Linux and service evidence

KR independently verified exact candidate `54f09f6` on Linux ARM64:
Node 22.23.2 and 24.16.0 each passed npm ci, typecheck, build, 52 tests and
production dependency audit (zero reported vulnerabilities). Python 3.12.3 passed
all 401 tests with the three then-current Node integration tests enabled. The
original KR source branch remained unchanged. This is evidence for that earlier
candidate, not the later OAuth and initial-principal corrections.

Latest production-code candidate `9c62f9e` passed 402 Python tests on macOS,
55 Node tests, typecheck/build, and wheel/sdist builds. Its four integration tests
include the managed WebSocket path. The latest KR service sandbox separately
passed compiled migration, loopback health/UI/Host and unauthenticated checks,
cross-UID readiness, explicit replay initialization, replay preservation,
revision increase, and dependency start/stop behavior. Its provider-transport
fixtures passed 18 OAuth/discovery tests; actual provider login remains untested.

The sandbox found two deployment requirements: the Node executable must be a
root-owned regular runtime outside protected home paths, and relay replay state
must use a private child directory rather than the DynamicUser top-level symlink.
The staged units were adjusted accordingly. A control crash currently stops the
relay; control recovery alone did not restart it. Automatic recovery is still a
gate, rather than treating a manual start as automatic recovery. Sandbox units
and listeners were stopped/removed, and the shared Caddy configuration stayed
unchanged. These checks used synthetic credentials and isolated state, not
production OAuth credentials or real agent sessions.

Latest exact candidate `9c62f9e` subsequently passed independent KR Linux ARM64
validation: Node 22.23.2 and 24.16.0 each passed clean install, typecheck, build,
55 tests and production audit with zero reported vulnerabilities. Python 3.12.3
passed all 402 tests, including the four then-current integration cases, in
35.992 seconds. Source bytes, lockfile and original KR branch were preserved.
These results came from the independent KR session, not a second macOS run.

A later test-only extension exercises rotation on the managed WebSocket route
itself. After the endpoint commits a key change, the fixture closes its real
WebSocket before returning the response. Reusing the operation ID reconciles the
control receipt and endpoint state, activates the new key, and preserves the
native message receipt: resending the same message ID yields one effect total.
The four focused integration cases passed in 5.267 seconds on macOS. Deliberate
recovery phases are spaced beyond the relay's 20-handshake-per-second budget;
the production rate limit is neither raised nor bypassed. This fixture still
replaces external TLS with loopback and does not establish public WSS or OAuth.

## Final campaign decision and operational handoff

The user waived the 24-hour test and requested termination of the loopback and
network test processes. The endpoint campaign ended at
**2026-09-18 14:38:54.010369 UTC** (23:38:54 KST), after 19,253.8356 seconds.
Its final status is `interrupted`: 321 cycles, 321 direct and 321 relay probe
successes, 65 new-message and 65 duplicate successes, and 772 public WebSocket
upgrades. Recorded failures, safety failures and sample gaps are zero. Both
`passed24Hours` and `releaseGatePassed` remain false. This is partial observation
evidence, not a completed 24-hour pass.

The local launcher/endpoint harness and the KR observer service/timer were
stopped. The earlier reauthorization schedule was cancelled with the campaign;
no further test or credential renewal is scheduled by this handoff. The KR
control/relay services remain separate from those terminated test processes.
The CF infrastructure adoption plan was discarded and is excluded from this
KR implementation branch and push.

Secret-free copies of the reviewed KR watchdog/readiness/relay units and helper
are preserved in [deploy/kr](../../deploy/kr/README.md). They do not include
credentials, environment files, provider subject IDs, databases or replay state.
The recorded actual OAuth, transcript ACK and service fault evidence remains
bounded by the scopes above. Stopping the soak does not broaden those scopes,
and this source push is not an RC release approval.

## Final RC rerun — September 19 (24-hour test excluded)

The user requested a complete RC rerun with only the continuous 24-hour test
excluded. The final candidate was exercised on macOS Python 3.14.7 and against
the deployed KR services. The control project passed `npm ci`, typecheck, build,
63 tests in eight files and `npm audit --omit=dev` with zero reported
vulnerabilities. The Python suite passed all 405 tests with the real compiled
Node control integration enabled. A fresh sdist and wheel were built; the wheel
installed with relay and MCP extras into a new virtual environment, `pip check`
passed, the installed CLI reported 0.9.0 and a clean-home list smoke passed.
Source-stream execution over SSH to KR returned a structured successful listing.

The rerun exposed and corrected two Antigravity 1.2.7 compatibility failures.
The native `agentapi send-message` command no longer accepts an extra `--` before
its recipient and message positionals. The bridge also compared a locale-formatted
`ps lstart` string, so a bridge launched in a Korean TUI disappeared when a relay
worker used the C locale. The command now uses the documented positionals and
process-start identity is always read with `LC_ALL=C`. Exact argv and stable-locale
regressions were added. Unified-list tests now explicitly isolate optional live
Antigravity registrations, and the older Claude permission-error test was aligned
with the current fail-closed discovery contract.

Fresh, unique request IDs were used for each native attempt. Same-Mac direct and
public-relay delivery passed for Claude 2.1.277, an isolated Codex 0.155.0 session
using `gpt-5.6-sol high`, and Antigravity CLI 1.2.7 using Gemini 3.8 Flash High.
Each public relay exchange observed receiver attachment and native submission or
queue acceptance. Each exact marker appeared once in the corresponding assistant
or model transcript. These transcript observations remain independent evidence;
the product API continues to report `consumptionConfirmed=false`.

Immediately before fault injection, control, readiness, relay and stack units
were active, health returned 200, and public-state/replay high-water revisions
both equaled 1183. Whole-process `SIGSTOP` triggered the configured 30-second
watchdog. In 35.67 seconds, control restarted under a new PID, readiness ran
again and relay restarted under a new PID without manual recovery. Configuration
hashes remained unchanged; state and replay revisions advanced
to 1185 without regression, both loopback listeners returned, and health returned
200. A new Claude public-relay request after recovery received one exact transcript
ACK. At the final idle sample control used about 39.8 MiB with 11 tasks and relay
about 19.8 MiB with one task; there were no established 3769/3770 connections.
The three recent control warnings were the expected injected watchdog timeout,
SIGKILL and failed-result records; relay had no warning-level lines.

The actual GitHub and Google callbacks, provider owner isolation, native device
authorization, enrollment/reconciliation, paired public WSS/E2EE, invalid-proof
and replay rejection, and active-connection revoke evidence in earlier sections
remain applicable to the unchanged deployed backend. The final rerun did not
repeat provider consent or mutate provider identities. No merge, release tag,
package publication or 24-hour campaign was performed.

## KR boot activation and infrastructure reboot — September 19

The boot policy enables only `session-peer-stack.target` from
`multi-user.target`. The control and relay services remain disabled as direct
boot entry points; the target starts control, and the existing Upholds,
readiness, Requires and BindsTo relationships establish the control → readiness
→ relay order. Migration and replay initialization remain manual, inactive
one-shot operations and are not boot dependencies.

An actual KR host reboot changed the boot ID from
`2537e227-6dc3-4137-85c6-bd253e4f8e5f` to
`5523b4cb-68d6-40a0-8987-b1cf084857f6`. SSH returned in about 28 seconds.
Control started at 12:37:38 KST, passed its health and state gate at 12:37:49,
readiness completed, and relay started at 12:37:49 without manual intervention.
All four stack units were active with zero restarts and no failed system units.
Public health and login returned 200; unauthenticated session and challenge
requests remained denied with 401 and 403 respectively.

The auth-state revision advanced from 1205 to 1207. The replay file retained
its two spent entries and original `notBefore`, then advanced its high-water
revision from 1185 to 1207 on relay startup. No migration, replay initialization,
credential replacement or database reset ran during the reboot.

Existing infrastructure was checked after boot: Caddy, Authelia, Authadmin,
Ontology, portfolio dashboard, server monitor, Orca, Redis, both rclone mounts,
Tailscale, SSH, OCI monitoring and the central Vault runtime were active. The
two rclone mounts were mounted and readable. Public ontology, chat, login and
status routes returned their expected 302/200/200/302 statuses, and `vault
status` reported `integrity=ok`. No containers were expected or running. The
only boot warnings were pre-existing kernel/platform and OCI unit-mode warnings;
the boot produced no failed units.

## Release gates

1. The shared enrollment, proof, rotation, generation, room, certificate
   fingerprint and public-state revision contract passed the final integrated suite.
2. Actual GitHub/Google callbacks, explicit device approval, owner isolation and
   revocation through the React interface passed in the recorded production pilot.
3. The fixed artifacts, KR isolation/readiness/state persistence, Caddy ingress
   and watchdog recovery passed with configuration and service evidence preserved.
4. The user waived the continuous 24-hour requirement. Preserve the interrupted
   campaign evidence; complete remaining agreed fault/recovery and platform
   checks separately without treating the interrupted campaign as a pass.
5. Real target transcript/ACK evidence for isolated Claude, Codex and Antigravity
   passed in the final rerun. Preserve the unique request records and do not
   resend any unknown historical attempt.
6. Submit an RC release PR only when agreed gates pass. This authorization does
   not include merging the release PR, creating a release tag or publishing PyPI.

Neither fixture socket writes nor Codex queue acceptance proves consumption.
Retain unknown outcomes and original operation IDs; never re-submit merely to
turn an unknown result into a success report.
