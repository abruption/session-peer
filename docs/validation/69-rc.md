# #69 RC validation record (in progress)

This is a development record, not an RC release approval. The KR infrastructure
operator reports public ingress and a real GitHub browser login below. Native
CLI authorization is blocked at Cloudflare; end-to-end public relay validation
and the 24-hour soak are not complete.

## Candidate and scope

The subsequent control watchdog change supersedes `9c62f9e` as a production-code
candidate; its previous results remain evidence for that exact revision only.
KR found that `Upholds` recovered exited processes but left a SIGSTOP-frozen
control and relay active. The application now gates systemd notifications on
listener and durable public-state health; see [watchdog operation](../relay-watchdog.md).
Local typecheck/build and 63 control tests pass. The later actual systemd recovery
and deployment report is recorded below, separately from the local mocks.
No 24-hour soak has started.

The KR validator subsequently reported independent checks of exact watchdog
candidate `428575a11799e4eb15b43e86aa6ceebe648dd7d4`: clean npm installations,
typecheck/build and 63 tests each on Linux ARM64 Node 22.23.2 and 24.16.0;
Python 3.12.3 installation/pip check and all four Node/Python integration tests
passed (10.351 seconds). Archive bytes, symlinks and lockfile stayed unchanged.
Those integration tests include the managed WebSocket disconnect after rotation
commit, same-operation recovery/deduplication and open-connection revocation.
These use synthetic credentials and native fixtures, not live OAuth or models.
The earlier 402-test suite, audit and release smoke were not repeated in this
limited follow-up and must not be attributed to this exact revision.

Reported KR evidence: `/home/ubuntu/session-peer-69-verify-428575a-ja2rrmku/verification-result.json`,
SHA256 `a263539246e473c9251e9d0d7ac5a1afafd44b5010031e0df391533fd7a3b7a3`.
This independent report does not establish systemd notification attribution,
SIGSTOP recovery, production application or the public ingress gate.

## Actual KR watchdog and initial public ingress (operator report)

The Mac ontology infrastructure operator subsequently tested exact `428575a` in
an independent KR DynamicUser/state sandbox and reported all runtime gates passed:
Node MAINPID/WATCHDOG_PID attribution, notification acknowledgement, 35.02 seconds
healthy operation, control/relay crash recovery, and fresh readiness before relay
restart. Whole-cgroup SIGSTOP reached watchdog failure in approximately 29.95 seconds;
persistent public-state mismatch produced health 503 and watchdog failure in
approximately 32.43 seconds. Both automatically recovered control and relay.
Explicit stack stop remained stopped. The configured 300-second/five-start limit
blocked the separate repeated-failure test. Keys, replay evidence and revision
history were preserved; migration/replay initialization were not automatic.

Evidence: `/opt/session-peer-watchdog-test.yqQsro/watchdog-result.json`, SHA256
`e616e0afb74d70401f6a45e6659d03ee2d67c37223d5b9794a4f534da71ab746`.
These are the operator's actual systemd observations, not a claim that the root
session independently repeated fault injection on production.

The same operator installed immutable `428575a` releases and the reviewed
watchdog/stack units, preserving existing state and taking a protected consistent
auth database snapshot. No migration, replay reinitialization or boot enablement
was performed. Production readiness and 35-second healthy operation passed.
The managed Caddy block was applied after actual-caddy-user validation and a live
configuration hash check. Existing site baselines remained unchanged. Caddy hash:
`78596de9a349a7df94226591830f40de4344a2b3b04d59a7b020d8425b25bf95`.
The operator observed an actual GitHub consent/callback reaching the authenticated
empty device page. Google remains disabled pending verified-sub discovery.

Current blocker: native `POST /api/auth/device/code` is rejected by Cloudflare.
The root session independently reproduced one request on 2026-09-18 at 08:08:02Z:
HTTP 403, Cloudflare error 1010 (`browser_signature_banned`), Ray
`a3cedc4558521c66-HKG`. The response declares it non-retryable. No browser-cookie
transfer, disguised user-agent or repeated attempts were used. CLI login token
creation, code approval, device enrollment and authenticated public WSS remain
unverified. A successful browser login or `/healthz` GET is not evidence for them.
The exact Ray was subsequently matched to a Cloudflare security event with
`source=bic`, `ruleId=bic`, `action=block`. After explicit user approval, the
operator added one BIC-only Configuration Rule for HTTPS POSTs without a query to
the exact `/api/auth/device/code` and `/api/auth/device/token` paths on
`relay.abruption.dev`. See [the edge policy](../relay-edge-policy.md).
Rule `7c74849bd67d4efaa459864ee7eaf8dc` belongs to ruleset
`d63e46bcdd88453bae8f67c2ecbba8a7`, both version 1 at creation.
The root session read the operator's saved result and Trace records: both intended
paths matched, while six excluded cases did not. Other protection was reported
unchanged. Live native authorization and downstream route results remain separate
from this configuration check; no other route exception is included.

Production result files under
`/var/lib/session-peer-deployments/428575a11799e4eb15b43e86aa6ceebe648dd7d4/`:
`watchdog-loopback-result.json` SHA256
`e1863c396888721ee25c7e357f83f531df2983705c5ee00793e3a7770f98578d`, and
`caddy-public-result.json` SHA256
`535617e7a8d8593806327a37fb5eea151208fd471c8375602b9cf71508ea2f33`.
No actual agent/Claude call, 24-hour campaign, RC publication, firewall/DNS change,
zone-protection relaxation or backup-job change is part of this report.

Base: `0dd9592d8dfddc56ecbca54072a8733afd998d37` (v0.9.0 plus documentation).
Development branch: `feat/69-relay-rc-hardening`.
The original working checkout is preserved. The control service is a separately
built Node application; installing the Python CLI does not install or start it.

The approved RC scope includes React, Better Auth, GitHub and Google login,
device ownership and device-bound admission, key lifecycle, and a private KR
deployment behind the existing Caddy ingress. Initial accounts are allowlisted.
The public hostname does not imply open registration or a hosted service offer.

## Acceptance mapping

| Issue #69 criterion | Implementation / test evidence | Remaining RC evidence |
| --- | --- | --- |
| Default local-only operation and local/SSH compatibility | Existing CLI default routing and Python regression suite | Candidate package smoke on supported hosts |
| Permitted native targets through direct route | `app.py`, `native.py`, `test_native_relay.py` | Isolated real Codex, Antigravity and Claude receive/ACK |
| Same identity via opaque relay | TLS 1.3 endpoint stream, `wire.py`, ciphertext/native fixtures | Managed public WSS route on actual devices |
| Structured submission stages | Existing `submitted`, `receiverAccepted`, `relayAttached`, `consumptionConfirmed` fields | Capture real submit and independent ACK separately |
| Concurrent route selection | `exchange()` probes verified candidates before one submission | Fault and VPN-off route observations |
| Shared versioned contract and splice readiness | `wire.py`, `app.py` envelope validation, relay attach tests | Node/Python admission compatibility and public handshake |
| Pairing/replay/revocation/rotation/limits | Native/relay, lifecycle and admission tests | Fixed candidate fault campaign during soak |
| Lost response reconciliation | Stable principal journal, rotation operation ID and status | Cross-language registration/rotation and real reconnect |
| Relay outage isolation | Direct fallback and CLI regression tests | Public service stop/restart with explicit SSH smoke |
| Platform lifecycle | Relay extra supports Unix/Python 3.11+, explicit management commands | macOS and Linux candidate lifecycle; Windows relay remains unsupported |
| Threat model/setup/removal/recovery | `paired-devices.md`, `relay-lifecycle.md`, `relay-auth.md` | Final deployed unit/config/runbook revision and recovery rehearsal |

Windows keeps core local/SSH support; this change does not claim Windows receiver
or relay support. The documented Unix-only boundary must remain explicit in the
RC description, rather than marking Windows relay integration as passed.

## Local findings and corrections

The independent ontology review reproduced three failures: restore could expose
state before quarantine, restored senders could still send/rotate, and a missing
relay replay file could be recreated automatically. The Python correction
`52d32c6` stages restoration before publication, checks both sender and receiver
recovery boundaries, and requires explicit durable replay initialization.

Regression coverage also checks an interrupted restore, retained status lookup,
missing/corrupt replay state, bootstrap ticket fencing, failure to persist a spent
ticket, and rollback of public state across restart. A complete machine rollback
of all authorities cannot be detected by a high-water mark on that same machine;
host-loss recovery still requires external reconciliation/fencing.

Local macOS/Python 3.13 run after this correction: 399 discovered tests, 396
passed and 3 Node integration tests explicitly skipped pending the common API
contract. Wheel and sdist builds passed. The initial Node candidate independently
passed typecheck/build and 31 tests on macOS; KR reported the same 31 tests on
Linux ARM64 Node 22/24. These counts are not live OAuth or model validation.

The exact Python commit `52d32c6` was also archived and installed into an isolated
KR Ubuntu ARM64/Python 3.12 virtual environment with relay and MCP extras. All
396 tests passed there in 36.611 seconds. The temporary source/environment was
removed after preserving the test log. No model call, production receiver,
relay service or Caddy change was part of this run.

Node/Python tests under `tests/test_control_integration.py` use the real control
application, Better Auth, SQLite and cryptography with disposable seeded sessions.
Their loopback HTTP bridge replaces only the external TLS transport. No test
authentication endpoint is added to the production application. The integrated
candidate initially passed three tests: generation-zero enrollment/admission/revocation,
managed key rotation with lost commit response and native-effect deduplication,
and cross-owner receiver rejection. The shared contract omits expectedGeneration
on first registration and uses expectedGeneration=0/new generation=1 on first
rotation. Historical completed operations reconcile via the owner-only receipt.

The subsequent macOS candidate run passed all 401 Python tests with integration
enabled, 52 Node tests, typecheck/build and a production npm audit (zero reported
vulnerabilities). Producer regressions include restrictive umask, separate-DB
restart, a directory-fsync failure after visible publication, revision exhaustion
and restored/missing revision refusal. Google bootstrap tests include partial
write/file-fsync/link failures, ambiguous directory persistence and explicit
operator confirmation. These are fixture results, not live provider login.

A fourth cross-language test then passed the complete managed admission → cookie
→ WebSocket → pinned endpoint TLS → native fixture path. The captured relay frames
did not contain the marker, and control revocation closed an already-open sender
connection and released its relay slot. Only outer network TLS was redirected to
loopback; this does not prove public WSS. Fresh wheel and sdist core installs also
passed version/help smoke, and the no-extras suite passed 338 tests (8 optional
module skips).

KR producer work df66f9a/1336ee0 supplied the public-directory permissions and
revision fault tests, integrated with the Python generation contract and existing
public-file rollback guard. The final integrated candidate still needs independent
Linux verification and actual sandbox deployment.

A further callback regression reproduced an unallowlisted identity leaving an
orphan user row even though account creation and login were refused. Both GitHub
and verified Google user-info handlers now check the immutable provider allowlist
before Better Auth creates a user; database hooks remain a second boundary.
Regression assertions cover unchanged user/account/session counts for both
providers. KR's subsequent 3de5604 also contributed certificate-derived initial
principal validation and a schema marker that refuses populated pre-contract
candidate state. The resulting Node suite passed 55 tests and all four Python
integration cases passed again. These findings supersede the earlier candidate
for actual OAuth deployment.

## Independent Linux and service evidence

KR independently verified exact candidate `54f09f6` on Linux ARM64:
Node 22.23.2 and 24.16.0 each passed npm ci, typecheck, build, 52 tests and
production dependency audit (zero reported vulnerabilities). Python 3.12.3 passed
all 401 tests with the three then-current Node integration tests enabled. The
original KR source branch remained unchanged. This is evidence for that earlier
candidate, not the later OAuth and initial-principal corrections.

Latest production-code candidate `9c62f9e` passed 402 Python tests on macOS,
55 Node tests, typecheck/build, and wheel/sdist builds. Its four integration tests
include the managed WebSocket path. The latest KR service sandbox separately
passed compiled migration, loopback health/UI/Host and unauthenticated checks,
cross-UID readiness, explicit replay initialization, replay preservation,
revision increase, and dependency start/stop behavior. Its provider-transport
fixtures passed 18 OAuth/discovery tests; actual provider login remains untested.

The sandbox found two deployment requirements: the Node executable must be a
root-owned regular runtime outside protected home paths, and relay replay state
must use a private child directory rather than the DynamicUser top-level symlink.
The staged units were adjusted accordingly. A control crash currently stops the
relay; control recovery alone did not restart it. Automatic recovery is still a
gate, rather than treating a manual start as automatic recovery. Sandbox units
and listeners were stopped/removed, and the shared Caddy configuration stayed
unchanged. These checks used synthetic credentials and isolated state, not
production OAuth credentials or real agent sessions.

Latest exact candidate `9c62f9e` subsequently passed independent KR Linux ARM64
validation: Node 22.23.2 and 24.16.0 each passed clean install, typecheck, build,
55 tests and production audit with zero reported vulnerabilities. Python 3.12.3
passed all 402 tests, including the four then-current integration cases, in
35.992 seconds. Source bytes, lockfile and original KR branch were preserved.
These results came from the independent KR session, not a second macOS run.

A later test-only extension exercises rotation on the managed WebSocket route
itself. After the endpoint commits a key change, the fixture closes its real
WebSocket before returning the response. Reusing the operation ID reconciles the
control receipt and endpoint state, activates the new key, and preserves the
native message receipt: resending the same message ID yields one effect total.
The four focused integration cases passed in 5.267 seconds on macOS. Deliberate
recovery phases are spaced beyond the relay's 20-handshake-per-second budget;
the production rate limit is neither raised nor bypassed. This fixture still
replaces external TLS with loopback and does not establish public WSS or OAuth.

## Release gates

1. Integrate and verify the same enrollment, proof, rotation, generation, room,
   certificate fingerprint and public-state revision contract on both sides.
2. Complete actual GitHub/Google callbacks, explicit device approval, owner
   isolation, and revocation through the React interface.
3. Deploy the fixed artifacts with the reviewed KR isolation, readiness,
   state persistence, Caddy validation and rollback procedure. Record artifact
   hashes, configuration revision and baseline service checks.
4. Run one continuous 24-hour campaign with planned faults and separate cold/warm
   evidence. Record exact UTC start/end, candidate changes and interruptions.
5. Obtain real target transcript/ACK evidence for isolated supported agents.
   Claude's quota is exhausted: do not invoke or repeatedly retry Claude. Its
   required real validation remains pending until quota recovery.
6. Submit an RC release PR only when agreed gates pass. This authorization does
   not include merging the release PR, creating a release tag or publishing PyPI.

Neither fixture socket writes nor Codex queue acceptance proves consumption.
Retain unknown outcomes and original operation IDs; never re-submit merely to
turn an unknown result into a success report.
